[test]
# Runs once before any test file — points the app at skynet_test and strips the
# LLM/embedding keys so distillation degrades to its built-in stub (no network).
preload = ["./test/preload.ts"]
