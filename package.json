{
  "name": "useagent",
  "license": "AGPL-3.0-only",
  "private": true,
  "scripts": {
    "dev:frontend": "bun run --cwd frontend dev",
    "dev:backend": "bun run --cwd backend dev",
    "typecheck": "bun run --cwd frontend typecheck && (cd backend && bunx tsc --noEmit) && bun run --cwd backend typecheck:release && (cd packages/sandbox-contract && bunx tsc --noEmit) && (cd packages/artifact-workspace && bunx tsc --noEmit) && (cd packages/artifact-formats && bunx tsc --noEmit) && (cd packages/agent-harness && bunx tsc --noEmit) && (cd packages/agent-client && bunx tsc --noEmit) && (cd packages/conformance && bunx tsc --noEmit) && (cd packages/cli && bunx tsc --noEmit)"
  }
}
